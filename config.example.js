window.SMOKING_NAV_CONFIG = {
  // Optional. Use this after you apply for an Environment Ministry data API key.
  moenvApiKey: "-------------------------",

  // Optional. Replace with your own backend endpoint when AI/RAG is implemented.
  aiChatEndpoint: "",

  // Optional. Replace with OSRM, Mapbox Directions, Google Directions, or your own routing API.
  routingEndpoint: "-------------------------"
};
